name: Journal Prices (US + EU + Asia)

on:
  workflow_dispatch:
  schedule:
    - cron: "45 10 * * *"

permissions:
  contents: read

concurrency:
  group: journal-prices
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        region: [us, eu, asia]
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Refresh one region
        working-directory: Albion_ProfitChecker/ui
        run: node scripts/refresh-journal-prices.mjs "${{ matrix.region }}"
      - uses: actions/upload-artifact@v4
        with:
          name: journals-${{ matrix.region }}
          path: Albion_ProfitChecker/ui/public/data/journals-${{ matrix.region }}.json
          if-no-files-found: error
  publish:
    needs: collect
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: journals-*
          path: Albion_ProfitChecker/ui/public/data
          merge-multiple: true
      - name: Commit outputs once
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Albion_ProfitChecker/ui/public/data/journals-*.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: refresh journal prices"
            git pull --rebase origin "$(git branch --show-current)"
            git push origin "HEAD:$(git branch --show-current)"
          fi
