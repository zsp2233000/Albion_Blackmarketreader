name: Build Material Prices by City (US + EU + Asia)

on:
  workflow_dispatch:
    inputs:
      region:
        description: "要抓取的地區"
        required: true
        default: asia
        type: choice
        options:
          - asia
          - us
          - eu
          - all
  schedule:
    - cron: "35 10 * * *"

permissions:
  contents: read

concurrency:
  group: materials-prices-by-city
  cancel-in-progress: false

jobs:
  collect:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        region: ${{ fromJSON(inputs.region == 'all' && '["us", "eu", "asia"]' || format('["{0}"]', inputs.region || 'asia')) }}
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Refresh one region
        working-directory: Albion_ProfitChecker/ui
        run: node scripts/refresh-raw-material-prices.mjs "${{ matrix.region }}"
      - uses: actions/upload-artifact@v4
        with:
          name: raw-materials-${{ matrix.region }}
          path: Albion_ProfitChecker/ui/public/data/raw-materials-cities-${{ matrix.region }}.json
          if-no-files-found: error
  publish:
    needs: collect
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: raw-materials-*
          path: Albion_ProfitChecker/ui/public/data
          merge-multiple: true
      - name: Commit outputs once
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Albion_ProfitChecker/ui/public/data/raw-materials-cities-*.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update regional raw material prices"
            git pull --rebase origin "$(git branch --show-current)"
            git push origin "HEAD:$(git branch --show-current)"
          fi
