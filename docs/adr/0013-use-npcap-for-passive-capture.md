# Use Npcap for passive packet capture

**Status: accepted**

The first version uses Npcap or an equivalent local packet-capture driver as a one-time installation prerequisite. The collector only reads traffic already generated by the game and never modifies, injects, or replays packets. If the driver is unavailable or capture permission is missing, the application continues with the existing Albion Data API fallback.
