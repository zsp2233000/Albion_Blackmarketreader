# Capture all, filter at calculation

**Status: accepted**

The collector retains every successfully parsed Black Market order rather than filtering against the current item catalog. Calculation and presentation apply the supported-item, quality, freshness, and validity rules. This keeps capture independent from the website catalog and allows new items to become usable without changing the packet collector.
